package br.com.tendencyapp.streamingLog

/*import org.apache.spark.logging

object StreamingLog extends Logging {
  
  def setStreamingLogLevels() {
    val log4jInitialized = Logger.getRootLoger.getAllAppenders.hasMoreElements
      if(!log4jInitialized){
        logInfo("Set log info to [WARN] for streaming")
        Logger.getRootLoger.setLevel(level.WARN)
      }
  }
}*/